name: flutter_samples
description: A new Flutter application.
version: 3.1.7+51
environment:
  sdk: ">=3.4.0"
dependencies:
  flutter:
    sdk: flutter

  http: ^1.2.2
  mime: ^1.0.2
  shared_preferences: ^2.0.15
  material_design_icons_flutter: ^5.0.6996
  flutter_styled_toast: ^2.1.3
  file_picker: ^8.1.2
  mobile_scanner: ^2.0.0
  flutter_speed_dial: ^4.1.0
  cached_network_image: ^3.4.1
  keyboard_actions: ^4.0.1
  intl: ^0.19.0
  image_picker: ^1.1.2
  image: ^4.2.0
  barcode_scan2: ^4.3.3
  flutter_form_builder: ^9.1.0
  form_builder_validators: ^11.0.0
  camera: any
  path: any
  path_provider: any
  camera_windows: ^0.2.5
  camera_platform_interface: ^2.1.2
  flutter_barcode_listener: ^0.1.3
  visibility_detector: ^0.3.3
  flutter_inappwebview: ^5.4.3+7
  flutter_email_sender: ^6.0.3
  package_info_plus: ^8.0.2
  url_launcher: ^6.1.5
  string_validator: ^0.3.0
  cupertino_icons: ^1.0.2
  mailto: ^2.0.0
  dropdownfield2: ^1.0.6

  http_parser: any
dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

#dependency_overrides:
#  date_range_picker:
#    git:
#      url: https://github.com/LempereurBenjamin/date_range_picker
#  intl: ^0.17.0-nullsafety.2

module:
  androidX: true

# The following section is specific to Flutter.
flutter:
  assets:
    - assets/images/ncnr.jpg
    - assets/images/Al-cans.jpg
    - assets/images/Brookhaven.jpg
    - assets/images/dcs-can.jpg
    - assets/images/V-cells.jpg
    - assets/images/blue_glow.jpg
    - assets/images/avatar.jpg
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
